spring:
  datasource:
    url: jdbc:mysql://localhost:3306/validationdb
    password: '!QAZ2wsx'
    username: root

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
